package oclminus.lexer;

public enum TokenType {
    INTEGER,
    IDENTIFIER,
    TRUE,
    FALSE,
    PLUS,
    STAR,
    DOT,
    EQUAL,
    ALL,
    LEFT_PAREN,
    RIGHT_PAREN,

    NOT_EQUAL,
    LESS_THAN,
    LESS_THAN_OR_EQUAL,
    GREATER_THAN,
    GREATER_THAN_OR_EQUAL,

    MINUS,
    SLASH,

    AND,
    OR,
    XOR,
    IMPLIES,
    NOT,

    EOF
}
